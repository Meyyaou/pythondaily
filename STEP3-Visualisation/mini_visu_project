#to be determined later from OC
import pandas as pd
import numpy as np

#exo
df= pd.read_csv('/content/operations.csv')
df.isnull().sum()

df.loc[df['montant'].isnull(), 'montant']= df['montant'].mean()
df['categ'].value_counts()
df.loc[df['categ'].isnull(), 'categ']= 'FACTURE TELEPHONE'
#corr matrix / zscore to check if max and min are good?
#df_heat= ['categ', 'libelle', 'montant', 'solde_avt_ope']
#corr= df.corr() 
min = df['montant'].min()
max = df['montant'].max()
min
max 
#sort it by date ope

